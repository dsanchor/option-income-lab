# Danny — 2026-08-29T12:30:21Z

**Role:** Lead  
**Assignment:** Best Options design (deterministic row filter: DTE + abs(delta) band)  
**Mode:** sync  
**Status:** COMPLETE — Ratified and revised (ACCEPTED → documentation correction)

## Outcome

**ACCEPTED.** Designed deterministic Best Options with two-stage row filter (DTE window 0-49 days, plus category-configured abs(delta) band). Design document originally misread delta band as color-only gate; Basher's review identified this as Defect 1 (missing durable decision record).

**Resolution:** Amended design doc in place (sections 2A/4.1/4.2) to clarify delta band as a true row-inclusion filter, and ratified via durable standalone record (`danny-best-options-delta-filter-correction.md`). No production code touched by this revision.

## Key Decisions

- **Row inclusion:** Primary `rows` contain all and only contracts satisfying (a) DTE window `[min, max]` and (b) abs(delta) in category's `[delta_lo, delta_hi]` band
- **Excluded contracts:** Feed `nearest_miss` and `excluded_by_delta_band` count metadata only; never silently dropped
- **Gate vs. filter:** Delta band is a filter (rows excluded); tradability (G1) and earnings span (G3) gates remain gates (rows colored red only)
- **No LLM in scoring, ranking, coloring, or availability**
- **Table design reuses Roll Scenarios:** Structure, spacing, typography, shared `ROW_TINT_BG` palette, expand/collapse idiom, non-color accessibility labels

## Cross-Agent Dependencies

- **Linus:** Implemented evaluator logic with delta-band filter; Basher's review confirmed implementation matches corrected semantics
- **Livingston:** Cache + contract tests
- **Rusty:** API endpoint + frontend UI; initial frontend rejected (contract mismatch), independent revision assigned to Livingston
- **Basher:** Adversarial testing; identified documentation gap, approved after amendment

## Reviewer Notes

Per Basher's final re-review: "Defect 1 — RESOLVED. `.squad/decisions.md` entry now durable and standalone. Defect 2 (frontend contract mismatch) remains Rusty's surface and unrelated to row-inclusion semantics."

