# Orchestration — Linus Merge Module Implementation (2026-08-18T06:43:00Z)

**Agent:** Linus (Quant Dev)  
**Duration:** Full session, parallel to Rusty  
**Activity:** Implemented frozen merge module; fixed provider normalizers; authored monotonicity/fuzz testing.

## Summary

**Frozen interface:** Seven pure functions in `backend/src/options_chain_merge.py` per Danny's design:
- `is_accepted(field, value)` — per-field validity predicate matrix
- `gate_contract(contract)` — trust-gate: contract passes if `ask > 0` or `iv > 0`
- `gate_bucket(bucket)` — degenerate-bucket rejection: ≥3 contracts, all failing gate
- `merge_sources(yf_chain, tv_chain)` — live-source phase: TradingView > yfinance, field by field
- `merge_prior(prior, live, *, now)` — accumulate: contract-level union + field-level overwrite-if-accepted
- `recompute_derived(chain, underlying_price, *, now)` — always fresh greeks from merged `iv` + current DTE
- `prune_by_expiration(chain, *, today_et)` — serve-time: drop expirations before today ET, never on TTL

**Provider normalizers:**
- `tv_options_chain_fetcher.py::_parse_tv_to_yfinance_format()` — Rule S1 (emit `None`, never fabricated zeros), Rule S3 (reject unparseable YYYYMMDD keys at ingestion).
- `yfinance_data_provider.py::_process_option_df()` (wired by Rusty) — identical Rule S1/S3 discipline; stopped emitting derived fields (recomputed only by `options_chain_merge.recompute_derived`).

**Tests:** `backend/tests/test_options_chain_merge.py` — 89 tests covering T1-T12 per design spec.

**Monot onicity finding:** Property test uncovered that T12 (`merge(merge(P,L1),L2) == merge(P,merge(L1,L2))`) holds cleanly when `L1`/`L2` are realistic `merge_sources` outputs (self-consistent quote groups by construction), but breaks on arbitrary synthetic dicts with inconsistent quote-group fields. Documented the refined guarantee with 300-seed fuzz test; no code change needed — `merge_prior` was always correct for real pipeline payloads.

**Calendar-date validation:** Added `strptime` validation to `_parse_tv_to_yfinance_format` so expiration keys are **doubly** validated (at TradingView ingestion *and* at `merge_sources` reconciliation) against malformed dates like `20261301` (month 13) or `20260230` (Feb 30), per Basher's review prompt.

## Constraints Honored

- Did NOT open `options_chain_cache.py` — boundary respected, Rusty owns integration.
- Did NOT add provider-side Cosmos/threading logic — pure module, zero side effects.
- All seven frozen functions are dependency-free and independently testable.

## Handoff

`backend/src/options_chain_merge.py` frozen for production. Rusty integrates via the seven-function interface; Livingston's revision later calls only into this module unchanged.
