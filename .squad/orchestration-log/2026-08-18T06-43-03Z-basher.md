# Orchestration — Basher Test Depth & Architecture Review (2026-08-18T06:43:03Z)

**Agent:** Basher (Tester)  
**Duration:** Full session, final gate  
**Activity:** Reviewed merge semantics and concurrency; identified D1-D5 defects; prescribed revision.

## Summary

**Merge semantics review:** Approved Linus's frozen `options_chain_merge.py` module and tests — all 89 tests passing, T1-T12 spec coverage complete, monotonicity property verified for realistic payloads, calendar-date validation strengthened.

**Composition review:** Identified five critical defects in Rusty's initial store/cache integration:
1. **D1/D2:** Store's CAS conflict reconciliation misusing `merge_prior` on already-merged chain.
2. **D3:** Hydrated data not pruned; missing top-level fields.
3. **D4:** `threading.RLock` on event loop; reentrancy allowing lost updates.
4. **D5:** Change-detection hash dead code under fake, broken in production.

Each defect failed its corresponding acceptance test:
- D1: Hydrated contracts missing derived fields.
- D2: Provenance metadata overwritten on round-trip.
- D3: Expired contracts served as candidates.
- D4: Concurrent same-loop refreshes both running full fetch.
- D5: Unchanged market data caused shard rewrites (high RU cost).

**Defect classification:** Not individual bugs, but architectural issues at the store↔merge seam, requiring an engineer with authority over both domains.

**Revision directive:** Escalated to new specialist (Livingston) with explicit D1-D5 fix requirements, bounded scope (§2.1: authorized artifacts only), and frozen baselines (§2.2: merge, providers, filters, watchdog untouched).

## Revision Gate (Post-Livingston)

Reviewed Livingston's D1-D5 fixes:
- **D1/D2:** Store now uses verbatim contract-level union (`_reconcile_bucket`), never calling `merge_prior` on merged data.
- **D3:** `_hydrate_into_memory` applies same-day pruning, carries timestamp/underlying_price, stamps entries stale-eligible.
- **D4:** Replaced single `RLock` with task-level memoization (same-loop reuse) + per-symbol OS lock offloaded via executor (cross-thread wait never blocks loop).
- **D5:** Content hash now excludes volatile `_meta.last_seen`/`quote_asof`, includes `underlying_price` — change detection effective in production.

**Validation:** Ran old test suite (pre-edit) against new store first — 32/33 passed unchanged, confirming behavioral compatibility. New R1-R7 integration tests compose real store + real merge module, verifying every field/provenance/concurrency scenario end-to-end.

**Outcome:** Livingston's revision approved. 546 focused-suite tests (merge/store/cache/integration/filters/normalize) passing; full backend 1244 passing (20 unrelated pre-existing failures in frozen `test_yfinance_data_provider.py`).

## Test Coverage Additions (Basher review follow-up)

Requested additional edge coverage; Linus + Livingston expanded without opening frozen modules:
- Real deployment divergence (cold replica scenario).
- Cosmos ETag 409 vs 412 parity.
- Schema migration (legacy shard without `schema_version`).
- Monotonicity fuzz (40 seeds, real `merge_prior`).
- Degenerate-bucket boundary (3-contract threshold).
- TV partial-field precedence (IV-only or ask-only scenarios).
- Malformed expiration self-healing (confirmed, corrected initial assumption).
- Carried-forward shape + `executable_buyback_ask` real-call.

All tests passing. No regressions.
